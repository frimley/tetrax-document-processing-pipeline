# NPM Configuration
# Suppress some warnings and improve performance

# Reduce audit noise for development
audit-level=moderate

# Faster installs
prefer-offline=true
progress=false

# Fund messages
fund=false

# Legacy peer deps (reduces warnings)
legacy-peer-deps=false

# Suppress specific deprecation warnings
loglevel=warn