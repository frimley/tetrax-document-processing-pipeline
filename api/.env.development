# Development Environment Configuration
# This file can be safely committed to git as it contains only non-sensitive dev values

# Application
NODE_ENV=development
PORT=3002

# JWT Configuration (Mock values for development)
JWT_SECRET=dev-jwt-secret-key-not-for-production
JWT_EXPIRES_IN=24h

# Redis Configuration (Docker/Local)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# PostgreSQL Configuration (Docker/Local)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DATABASE=tetrax

# MinIO/S3 Configuration (Docker/Local)
AWS_ACCESS_KEY_ID=minio
AWS_SECRET_ACCESS_KEY=minio12345
AWS_REGION=us-east-1
S3_BUCKET=test-company-bucket
S3_ENDPOINT=http://localhost:9000
S3_FORCE_PATH_STYLE=true